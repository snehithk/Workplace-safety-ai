# Memory Log - April 3, 2026

## Events

- Explored Gemma 4 model options and requirements:
  - E2B (2B params, 4GB RAM, multimodal)
  - E4B (4B params, 5.5-12GB RAM, multimodal)
  - 26B-A4B (MoE, 16-18GB RAM)
  - 31B (31B params, 17-20GB RAM)
  - Current server has 7.8GB RAM → can run E2B or E4B
  - Comparison with current Step-3.5 Flash model (cloud-only, text-only, free tier)
- Researched and documented differences: privacy (local vs cloud), multimodal support, cost, context length, licensing
- Installed Tailscale on server to enable secure private networking
  - Fixed broken monarx APT repository issue (removed monarx.list)
  - Successfully installed Tailscale via install script
  - Attempted `tailscale up` – requires web authentication
  - Currently pending user authentication at https://login.tailscale.com/a/3ca991b01f0c3
- Enabled Telegram exec approvals correctly:
  - Corrected configuration: `channels.telegram.execApprovals` (not `tools.elevated`)
  - Set approvers: [6045828445] (user's numeric Telegram ID)
  - Target: "dm" (approval prompts sent to DM)
  - Gateway restart successful; config verified with `openclaw config get`
- Successfully used `/approve` in Telegram to authorize elevated commands
- Learned: OpenClaw exec approvals require `channels.telegram.execApprovals.enabled` and numeric user IDs
- Gateway status: running on 0.0.0.0:18789 (all interfaces)

## Decisions

- Will use Tailscale for secure private networking (instead of exposing gateway to public internet)
- Will install Gemma 4 E2B locally after Tailscale setup completes
- Keep Step-3.5 Flash as primary model for OpenClaw agent (via OpenRouter)
- Enable chat-based exec approvals for convenience

## Pending Tasks

- Complete Tailscale authentication (user must click auth link)
- After Tailscale auth: configure OpenClaw gateway to use Tailscale (`gateway.tailscale.mode=serve`)
- Get Tailscale IP and verify connectivity
- Optionally install Gemma 4 E2B via Ollama

## Technical Notes

- Tailscale install command: `curl -fsSL https://tailscale.com/install.sh | sh`
- Tailscale up generates auth URL; need user interaction to complete
- OpenClaw config path: `/root/.openclaw/openclaw.json`
- Telegram exec approvals require numeric user IDs, can resolve via `openclaw logs --follow`
- Gateway binding: currently `lan` but effectively `0.0.0.0` (all interfaces)